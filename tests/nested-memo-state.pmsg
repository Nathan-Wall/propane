// Nested message types for testing memo behavior with state persistence

// @message
export type InnerMessage = {
  '1:value': string;
};

// @message
export type OuterMessage = {
  '1:counter': number;
  '2:inner': InnerMessage;
};
