import { Message } from '@propanejs/runtime';

// Nested message types for testing memo behavior with state persistence

export type InnerMessage = Message<{
  '1:value': string;
}>;

export type OuterMessage = Message<{
  '1:counter': number;
  '2:inner': InnerMessage;
}>;
