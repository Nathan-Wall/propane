import { Brand } from '../common/types/brand';
import { Email } from './email.propane';
import { Hash } from './hash.propane';

export type User = {
  name: string;
  email: Email;
  passwordHash: Hash;
  created: Date;
  updated: Date;
};
